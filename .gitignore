# Never commit real secrets — only .env.example (the template) should be in the repo
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / Next.js (for when we build the frontend)
node_modules/
.next/
out/

# OS junk
.DS_Store
Thumbs.db
